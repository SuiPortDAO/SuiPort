import 'package:flutter/material.dart';

class NFTsPage extends StatelessWidget {
  const NFTsPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
